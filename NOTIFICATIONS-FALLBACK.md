# Notifications Fallback

`scripts/clickup.sh` appends here automatically whenever `CLICKUP_API_KEY`
or `CLICKUP_LIST_ID` is missing from the environment, so notification
history survives even without ClickUp configured. This file is committed
like any other memory file when it changes — it is not gitignored.

No fallback entries yet.
